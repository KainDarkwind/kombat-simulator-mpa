import React from "react";

export default function Actions(props) {
   return (
      <div className="col-10">
         <div className="d-inline font-weight-bold">{props.name}</div>
      </div>
   );
}
